# paywall

Приложение с системой подписок, включающее онбординг и оплату.

## 🏗️ Архитектура

**BLoC Pattern + Clean Architecture** с использованием следующих технологий:

- **State Management**: flutter_bloc + Cubit
- **Dependency Injection**: injectable + get_it  
- **Local Storage**: shared_preferences
- **Code Generation**: build_runner + injectable_generator
- **Immutable State**: equatable + sealed classes

### Архитектурные принципы

- **Модульность**: Каждая группа экранов - отдельный модуль (splash, onboarding, paywall, home), здесь получилось, что 1 экран = 1 модуль, но в будущем может быть несколько экранов в одном модуле
- **Sealed Classes**: Для type-safe state management
- **Dependency Injection**: Централизованное управление зависимостями
- **Single Responsibility**: Каждый класс отвечает за одну задачу

## 📁 Структура проекта
lib/
├── main.dart # Точка входа приложения
├── injection.dart # DI конфигурация
├── injection.config.dart # Сгенерированный DI код
│
├── internal/infra/ # Внутренняя инфраструктура
│ ├── models/ # Модели
│ └── services/ # Сервисы
│
├── onboarding/ # Модуль онбординга
│ ├── usecase/ # Управление состоянием экранов в модуле через кубиты
│ │ ├── onboarding/ 
│ │ ├──── onboarding_cubit.dart # Логика онбординга
│ │ └──── onboarding_state.dart # Состояния
│ └── ui/ # все виджеты модуля 
│ ├──── pages/ # все экраны модуля
│ ├────── onboarding_page.dart
│ └──── onboarding/ # все виджеты, относящиеся к экрану OnboardingPage
│ └────── onboarding_view.dart
│
├── splash/ # Модуль загрузочного экрана
│ ├── ...
│
├── paywall/ # Модуль оплаты
│ ├── ...
│
└── home/ # Модуль главного экрана
└──── ...

